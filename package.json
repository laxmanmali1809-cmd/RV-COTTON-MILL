{
  "name": "rv-cotton-mill-orders",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check client.js"
  },
  "engines": {
    "node": ">=20"
  }
}
